# v1.7.11b1 — BCM-300 / NR-10E 시험판

이 버전은 공개 코드와 사용자 확인을 바탕으로 BCM-300 제어·상태 엔티티를 추가합니다. 실제 보일러 통신은 개발 환경에서 검증하지 않았습니다.

## 추가 기능

- NR-10E 온수 단계: 저온 / 중온 / 고온 (R3: 0 / 1 / 2)
- 재실 / 외출 선택 및 예약 실행 스위치
- 연소, 오류, 보일러 연결, 물 보충 상태와 실내온도 센서
- 실내 10–40 °C, 온돌 40–80 °C 범위 검사(NR-10E 옵션)
- 공유 상태 조회, 명령 실패 처리, 원시 레지스터 진단 다운로드
- 기존 climate unique ID 및 기존 HVAC 모드 명령 유지

## HACS 설치

이 안내는 **GitHub 시험판 릴리스가 게시된 후** 적용됩니다. 이 문서가 커밋된 것만으로 릴리스가 게시되지는 않습니다.

1. HACS → 사용자 지정 저장소에 `https://github.com/gm2945/sihas-canary`를 추가합니다. 유형은 Integration입니다.
2. 해당 저장소의 시험판 표시를 켜고 `v1.7.11b1`을 선택하여 다운로드합니다. 기본 브랜치 main에는 이 기능이 아직 반영되지 않았습니다.
3. Home Assistant를 재시작합니다.
4. 설정 → 기기 및 서비스 → SiHAS의 BCM 구성에서 **경동 NR-10E 온수 3단계 사용**을 켭니다.

기존 원본 SiHAS를 HACS로 관리 중이면 같은 `sihas` 폴더를 사용하는 포크로 전환해야 합니다. 원본과 포크를 동시에 관리하지 마세요. 변경 전에 HA 백업을 권장합니다.

## 지원 범위

- 실내 / 온돌 / 온수는 climate의 `preset_mode` 속성에 읽기 전용으로 표시합니다. 모드 전환 명령은 지원하지 않습니다.
- 난방세기 약 / 중 / 강과 추가 물 이상 코드는 아직 미확인입니다.
- 온수 모드에서 온도(°C) 설정을 차단하고 온수 단계 엔티티를 사용합니다.
- 연소 ON은 온수 가열일 수도 있어 `hvac_action=heating`으로 단정하지 않습니다.

## 검증

Python 3.14.7 / Home Assistant 2026.9.3에서 모의 장치 테스트 27개 통과. 실기기 동작은 설치 후 확인해야 합니다.

[상세 설치·매핑·복원 안내](custom_components/sihas/docs/BCM300.md)
